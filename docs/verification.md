# 1.0.0 验证记录

- Windows 10 22H2 x64，125% DPI。
- 核心计时测试：19/19 通过。
- 原生界面回归：112/112 通过，包括图标中心对齐、窗口断点布局、热力图悬停和正方形格子、暂停/继续、启动圆环、任务读写与备份恢复。
- 解压包从另一工作目录启动，数据库正确生成在程序旁的 `data/xili.db`。
- 检查进程实际加载路径：.NET、WinUI、Win2D、SQLite 来自解压目录。
- 模拟运行中重启：剩余时间重置为完整设定时长，任务和专注历史保留，SQLite 完整性检查通过。
- 正式程序集不包含 UI 自动测试入口，ZIP 不包含数据库、个人备份、调试符号或日志。
- 原生 SQLite 3.53.3，NuGet 依赖审计未报告已知漏洞。

目前未在另一台实体电脑、Windows 11 或完全无开发环境的虚拟机上验证。已检查自包含依赖及异目录启动；不将这些检查描述为跨机器实测。
